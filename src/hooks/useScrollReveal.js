import { useEffect } from 'react';

// Scroll reveal animations have been removed per user preference.
export function useScrollReveal() {
  useEffect(() => {
    // No-op
  }, []);
}

export default useScrollReveal;
